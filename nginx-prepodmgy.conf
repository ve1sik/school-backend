server {
    listen 80;
    listen [::]:80;
    server_name prepodmgy.ru www.prepodmgy.ru;

    # Redirect HTTP → HTTPS (if you have SSL)
    # return 301 https://$host$request_uri;

    # ─── Backend API ───
    location /api/ {
        proxy_pass         http://127.0.0.1:3000/;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 60s;
    }

    # ─── Backend uploads/static (direct) ───
    location /uploads/ {
        proxy_pass         http://127.0.0.1:3000/uploads/;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        expires 7d;
    }

    # ─── Telegram webhook ───
    location /telegram/ {
        proxy_pass         http://127.0.0.1:3000/telegram/;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_read_timeout 65s;
    }

    # ─── Frontend SPA ───
    location / {
        proxy_pass         http://127.0.0.1:4173;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
        # важно для SPA-роутинга: отдаём index.html на любой путь
        proxy_intercept_errors on;
        error_page 404 = @fallback;
    }

    location @fallback {
        proxy_pass http://127.0.0.1:4173;
    }
}

# ─── HTTPS (раскомментировать после certbot) ───
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name prepodmgy.ru www.prepodmgy.ru;
#     ssl_certificate     /etc/letsencrypt/live/prepodmgy.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/prepodmgy.ru/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#
#     # Те же блоки location что выше
# }
