export declare class AiService {
    generateStrictReport(studentName: string, testsScore: number, writtenScore: number, oralScore: number, weakestTheme: string | null): Promise<string>;
    private generateSmartAnalytics;
}
